# PAL Institute backend — production dependencies only.
# Compatible with Python 3.10+ (Render pins 3.10.13 via runtime.txt).
#
# DO NOT add internal/non-PyPI packages here (e.g. emergentintegrations) —
# Render and other public hosts can't resolve them and the build will fail.

# Web framework + ASGI server
fastapi==0.110.1
uvicorn[standard]==0.25.0
gunicorn==23.0.0

# WSGI bridge for hosts that only support WSGI (e.g. PythonAnywhere).
# Render does not need this, but it is tiny and keeps deploys portable.
a2wsgi==1.10.10

# Database driver (sync — works under both ASGI and WSGI workers)
pymongo==4.6.3

# Core utilities
pydantic==2.6.4
python-dotenv==1.0.1
PyJWT==2.10.1
